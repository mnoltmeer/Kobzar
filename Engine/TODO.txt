[*] при експорті в xml не експортуються відповіді що не прив'язані до сцени

[+] додати метод Run(), який буде транслювати Скрипт та записувати результат в Result

[x] додати try-catch в методи з DlgClass

[+] доробити функції Link() та Unlink()

[+] дотестити функції розповіді історій
[+] краш при виборі відповіді (якщо їх багато) після завантаження з xml (баг в тестовому застосунку)
[+] неправильно працює перехід до сцени після другого рівня відповіді (баг в тестовому застосунку)

[+] замінити CardOfDialog на Dialog

[+] замінити LinkedID та LinkedFromID на PrevID та NextID

[+] протестувати функціонал створення Розповідей
[+] перевірити роботу Unlink()

[+] додано функцію RunScript до інтерфейсу KE_INTERFACE
[+] вирішити як передати до static функцій ELI вказівник на екземпляр KobzarEngine (через параметр ELI)
[+] перевірити роботу скриптів
[+] RunScript() має транслювати текст скрипту, текст елементу Скрипт має транслюватись функцією Run()
[+] вирішити де мають бути функції-обгортки: в бібліотеці рушія чи в окремій бібліотеці
    (в окремій)

[-] зробити бібліотеку візуалізації
[+] вирішити як робити батьківське вікно: створювати автоматично через WinAPI
    чи брати дескриптор з користувацього додатка
    (автоматично)
[+] додати функції малювання примітивів (ліній, точок, дуг)
[+] додати текст до об'єктів Frame та Bubble
[-] додати обробку кліків контролів
[+] бібліотека зависає під час створення контролу (ймовірно потрібен окремий потік для віконної процедури)
    ні, просто ф-я створення повертала інтрепретатору NULL
[+] переробити механізм створення дочірнього вікна для контролу через повідомлення + PostMessage + CreateEvent
[+] вікно контролу не оновлюється коректно при зміні розміру через старі параметри розміру
    в BITMAPINFO для прозорого фону
[+] всю логіку роботи з GDC контролів треба перенести в GUI-потік DLL
[+] вирішити як визначати в якому вікні малювати фігури
    через додатковий параметр $handle в функціях та методах ELI
[-] додати методи до класу VisualScene
    (визначення і зміна розмірів вікна, визначення центру і так далі)
[-] присутній плаваючий баг з промальовкою елементів
    можлива причина: надто часте перемальовування вікна


